// Substitua as linhas 76-80 pelo seguinte código:

<RiAddLine
  className={`w-6 h-6 text-gray-600 transition-transform duration-200 ${isOpen ? 'rotate-45' : ''
    }`}
/>
<span className="text-lg font-bold text-black">{question}</span>

// O código completo do botão deve ficar assim:
<button
  className="w-full py-6 text-left flex items-center gap-4 focus:outline-none"
  onClick={onClick}
>
  <RiAddLine
    className={`w-6 h-6 text-gray-600 transition-transform duration-200 ${isOpen ? 'rotate-45' : ''
      }`}
  />
  <span className="text-lg font-bold text-black">{question}</span>
</button>

